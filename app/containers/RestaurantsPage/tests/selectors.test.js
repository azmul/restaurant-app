// import { selectRestaurantsPageDomain } from '../selectors';

describe('selectRestaurantsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
